program password;

var input: integer;

begin
    write('enter password: ');
    if readln(input) then
    begin
        if input = 1234 then writeln('ACCESS GRANTED')
        else writeln('ACCESS DENIED')
    end
    else writeln('hint: the password is an integer')
end.